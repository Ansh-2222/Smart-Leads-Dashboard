# Backend host only — /api/v1 is appended automatically in axios.ts
# Dev: leave empty to use Vite proxy (server.proxy in vite.config.ts)
# Prod: set to your Render backend URL, e.g. https://smart-leads-dashboard-2z9r.onrender.com
VITE_API_URL=
